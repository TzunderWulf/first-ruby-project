<!DOCTYPE html>
<html>
  <head>
    <title>FirstApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="block">
    <div class="nav">
      <%= link_to "See all articles", articles_path, class: "nav-item" %>
      <%= link_to "Create new post", new_article_path, class: "nav-item" %>
    </div>
    <%= yield %>
  </body>
</html>
