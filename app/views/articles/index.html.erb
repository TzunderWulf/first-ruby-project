

<h1>Articles</h1>

<p>Our blog has <%= Article.public_count %> article(s).</p>

<div class="posts">
    <% @articles.each do |article| %>
        <% unless article.archived? %>
            <div class="post">
                <h2><%= link_to article.title, article %></h2>
            </div>
        <% end %>
    <% end %>
</div>
